package User;

import java.util.List;

public class Professor extends User{

    public Professor(String DNI, String name, String surname, int age, boolean debt) {
        super(DNI, name, surname, age, debt);
    }
}
