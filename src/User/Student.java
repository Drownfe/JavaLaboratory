package User;

import MainMenu.Menu;

import java.util.Scanner;

public class Student extends User {
    public Student(String DNI, String name, String surname, int age, boolean debt) {
        super(DNI, name, surname, age, debt);
    }
}

